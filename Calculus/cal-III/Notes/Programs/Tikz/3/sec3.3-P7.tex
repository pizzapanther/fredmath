\documentclass{article}
\usepackage{tikz}
\usepackage{verbatim}
\usepackage[active,tightpage]{preview}
\usepackage{pgfplots}
\usepackage[T1]{fontenc}
\PreviewEnvironment{tikzpicture}
\usetikzlibrary{arrows,snakes,shapes}
\setlength{\PreviewBorder}{5pt}
\usetikzlibrary{calc}

\begin{document}
\begin{tikzpicture} 

	\fill [gray!30] plot [domain=0 : 2*pi ] 	 (xy polar cs:angle=\x r, radius= {4*cos(\x r)}  );
	\fill [gray!30] plot [domain=0 : 2*pi ] 	 (xy polar cs:angle=\x r, radius= {2 }  );
	\draw [ line width=.7mm, color=blue, domain=0:2*pi, samples=200, smooth]
  		plot (xy polar cs:angle=\x r, radius={ 4*cos(\x r)} );

	\draw [ line width=.7mm, color=black!60!green, domain=0:2*pi, samples=200, smooth]
  		plot (xy polar cs:angle=\x r, radius={2} );

	\draw [blue] (2, 2.3) node {r=4cos$\theta$};
	\draw [ black!60!green ]  (-1, 2.1) node {\emph r=2};	
	
	\draw[->, ultra thick] (-2.5, 0) -- (4.5, 0);
	\draw[->,  ultra thick] (0, -2.5) -- (0, 3);

	\draw  (4.6, -.25) node {\emph x};
	\draw  (-.25, 3.1) node { \emph y};

	\draw  (1, -.1) -- (1, .1) ;
	\draw  (3, -.1) -- (3, .1) ;

	\draw  (4.2, -.3) node { 4};
	\draw  (-2.2, -.3) node { -2};
	\draw  (2.15, -.3) node { 2};

	\draw  (-.1, 1) --(.1, 1) ;
	\draw  (-.1, -1) --(.1, -1) ;
	\draw  (-.2, 2.2) node { 2};
	\draw  (-.2, -2.2) node { -2};

\end{tikzpicture}
\end{document}