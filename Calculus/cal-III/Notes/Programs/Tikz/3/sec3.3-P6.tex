\documentclass{article}
\usepackage{tikz}
\usepackage{verbatim}
\usepackage[active,tightpage]{preview}
\usepackage{pgfplots}
\usepackage[T1]{fontenc}
\PreviewEnvironment{tikzpicture}
\usetikzlibrary{arrows,snakes,shapes}
\setlength{\PreviewBorder}{5pt}
\usetikzlibrary{calc}

\begin{document}
\begin{tikzpicture} 

	\fill [gray!30] plot [domain=0 : pi /2]  (xy polar cs:angle=\x r, radius= {2-2*cos(\x r) }  );
	\fill [gray!30] plot [domain=pi/2 : pi ]  (xy polar cs:angle=\x r, radius= {2+2*cos(\x r) }  );
	\fill [gray!30] plot [domain=0 : -pi /2]  (xy polar cs:angle=\x r, radius= {2-2*cos(\x r) }  );
	\fill [gray!30] plot [domain=-pi/2 :- pi ]  (xy polar cs:angle=\x r, radius= {2+2*cos(\x r) }  );
	\draw [ line width=.7mm, color=blue, domain=0:2*pi, samples=200, smooth]
  		plot (xy polar cs:angle=\x r, radius={2-2*cos(\x r) } );

	\draw [ line width=.7mm, color=black!60!green, domain=0:2*pi, samples=200, smooth]
  		plot (xy polar cs:angle=\x r, radius={2+2*cos(\x r) } );

	\draw [black!60!green] (2, 2.8) node {r=2+2cos$\theta$};
	\draw [ blue ]  (-2, 2.8) node {r=2-2cos$\theta$};	
	
	\draw[->, ultra thick] (-4.4, 0) -- (4.5, 0);
	\draw[->,  ultra thick] (0, -3) -- (0, 3.5);

	\draw  (4.6, -.25) node {\Large \emph x};
	\draw  (-.25, 3.6) node { \Large \emph y};

	\draw  (1, -.1) -- (1, .1) ;
	\draw  (2, -.1) -- (2, .1) ;
	\draw  (3, -.1) -- (3, .1) ;
	\draw  (-1, -.1) -- (-1, .1) ;
	\draw  (-2, -.1) -- (-2, .1) ;
	\draw  (-3, -.1) -- (-3, .1) ;

	\draw  (4.1, -.3) node { 4};
	\draw  (-4.2, -.3) node { -4};

	\draw  (-.1, 1) --(.1, 1) ;
	\draw  (-.1, -1) --(.1, -1) ;
	\draw  (-.2, 2.4) node { 2};
	\draw  (-.2, -2.4) node { -2};

\end{tikzpicture}
\end{document}