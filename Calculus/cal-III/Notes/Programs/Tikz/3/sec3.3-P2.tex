\documentclass{minimal}
\usepackage{tikz}
\usepackage{verbatim}
\usepackage[active,tightpage]{preview}
\usepackage{pgfplots}
\usepackage[T1]{fontenc}
\PreviewEnvironment{tikzpicture}
\setlength{\PreviewBorder}{5pt}
\usetikzlibrary{calc}

\begin{document}
\begin{tikzpicture} 

	\fill [fill=gray!30] plot [domain=0 : pi/2 ] 	 (xy polar cs:angle=\x r, radius= {1 - cos(\x r)}  );
	\fill [fill=gray!30] plot [domain=-pi/2 : 0 ] 	 (xy polar cs:angle=\x r, radius= {1 - cos(\x r)}  );
	\draw [ line width=.7mm, color=blue, domain=0:2*pi, samples=200, smooth]
  		plot (xy polar cs:angle=\x r, radius={1 - cos(\x r)} );

	\fill [fill=gray!30] plot [domain=pi/2 : 3*pi/2 ] 	 (xy polar cs:angle=\x r, radius= {1} );
	\draw [ line width=.7mm, color=black!60!green, domain=0:2*pi, samples=200, smooth]
  		plot (xy polar cs:angle=\x r, radius={1} );

	\draw [blue] (-1.5, 1.6) node {r=1-cos$\theta$};
	\draw [ black!60!green ]  (1.25, 1) node {\emph r=1};	
	
	\draw[->, ultra thick] (-2.4, 0) -- (2, 0);
	\draw[->,  ultra thick] (0, -1.7) -- (0, 2);

	\draw  (2.1, -.25) node {\emph x};
	\draw  (.25, 2.1) node { \emph y};

	\draw  (1, -.1) --(1, .1) ;
	\draw  (-1, -.1) --(-1, .1) ;
	\draw  (1.1, -.3) node { 1};
	\draw  (-2.2, -.25) node { -2};
	\draw  (-1.15, -.3) node { -1};
	
	\draw  (-.1, 1) --(.1, 1) ;
%	\draw  (-.1, 2) --(.1, 2) ;
	\draw  (.2, 1.2) node { 1};
	%\draw  (.2, 2.2) node { 2};


\end{tikzpicture}
\end{document}