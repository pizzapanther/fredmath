\documentclass{article}
\usepackage{tikz}
\usepackage{verbatim}
\usepackage[active,tightpage]{preview}
\usepackage{pgfplots}
\usepackage[T1]{fontenc}
\PreviewEnvironment{tikzpicture}
\usetikzlibrary{arrows,snakes,shapes}
\setlength{\PreviewBorder}{5pt}
\usetikzlibrary{calc}

\begin{document}
\begin{tikzpicture} 

	\fill [fill=gray!30] plot [domain=0 : 2*pi ] 	 (xy polar cs:angle=\x r, radius= {1 + cos(\x r)}  );
	\fill [white] plot [domain=0 : 2*pi ] 	 (xy polar cs:angle=\x r, radius= {1 }  );
	\draw [ line width=.7mm, color=blue, domain=0:2*pi, samples=200, smooth]
  		plot (xy polar cs:angle=\x r, radius={1 + cos(\x r)} );


	\draw [ line width=.7mm, color=black!60!green, domain=0:2*pi, samples=200, smooth]
  		plot (xy polar cs:angle=\x r, radius={1} );

	\draw [blue] (1.3, 1.65) node {r=1+cos$\theta$};
	\draw [ black!60!green ]  (-1, 1) node {\emph r=1 };	
	
	\draw[->, ultra thick] (-1.7, 0) -- (2.5, 0);
	\draw[->,  ultra thick] (0, -1.7) -- (0, 2);

	\draw  (2.6, -.25) node {\emph x};
	\draw  (-.25, 2.1) node { \emph y};

	\draw  (1, -.1) --(1, .1) ;

	\draw  (1.1, -.3) node { 1};
	\draw  (-1.15, -.3) node { -1};
	\draw  (2.15, -.3) node { 2};

	\draw  (-.1, 1) --(.1, 1) ;
	\draw  (-.2, 1.2) node { 1};
	\draw  (-.2, -1.2) node { -1};

\end{tikzpicture}
\end{document}