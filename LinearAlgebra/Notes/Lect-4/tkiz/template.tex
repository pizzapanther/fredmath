\documentclass{article}
\usepackage{tikz}
\usetikzlibrary{arrows,snakes,shapes}
\usepackage{verbatim}
\usepackage{pst-circ}
\usepackage[active,tightpage]{preview}
\PreviewEnvironment{tikzpicture}
\usepackage{circuitikz}
\setlength{\PreviewBorder}{5pt}


\begin{comment}
:Title:  
\end{comment}

\begin{document}

\begin{circuitikz} % [ scale =1.1, american]
\begin{tikzpicture}


\tikzset{middlearrow/.style={
        decoration={markings,
            mark= at position 0.5 with {\arrow{#1}} ,
        },
        postaction={decorate}
    }
}
\draw [ dashed,  thick, middlearrow={>}]  (1.2, .8) -- (-.8, .8) ;


	\newcommand*{\equal}{=}
	
% arc
	\draw [very thick, ->] (2.5, .5) to [bend right] (2.5, 1.5);
	
	\draw [dashed, very thick] (0, 0) arc (180:0: 2 and .5);

% Semicircle using polar
		\draw [blue, domain=0: 180, very thick] plot(\x: 2 );

% ellipse
	\draw [very thick] (2, 4) ellipse (2 and .5) 

% Bold Italic
	\draw  (2, 1.5) node { \LARGE   \textbf  {\textit x(t) } } ;

% Fraction
	\draw [red] (2, 3.5) node {\large $\frac {\pi}{16}$ };
	\draw  (-.25, -.9) node  { \small $\sqrt {  x^2+m^2 }$} ;

% Function
	\draw[domain=0:5, variable=\x] plot ({\x},{-.16*\x*\x +\x + 1});

% Rotation
	[rotate=65] 

%degree
	\draw (4, -.25) node { \footnotesize  $40^\circ $ };
	\normalsize
	\small
	\footnotesize
	\scriptsize
	\tiny

Partial ellipse
	\tikzset{
    partial ellipse/.style args={#1:#2:#3}{
        insert path={+ (#1:#3) arc (#1:#2:#3)}
    }
}
	\draw [thick, blue] (2.5,0) [partial ellipse=180:0:2.5 and 2 ] ;

%
	\draw (1.6, 2) node { \Large $\nabla$ } ;

% less than absolute
  $|x|+|y|\leq1$
	
\end{tikzpicture} 
\end{circuitikz}
\end{document}